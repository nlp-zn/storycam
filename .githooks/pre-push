#!/usr/bin/env bash
set -euo pipefail

scripts/pr-ready.sh

cat <<'MSG'

Pre-push deterministic checks passed.

Before opening or merging the PR, ask Codex:

Run the StoryCam PR gate on the current diff.

Produce three independent reviewer reports:
1. code-reviewer: docs/pr-reviewers/code-reviewer.md
2. security-auditor: docs/pr-reviewers/security-auditor.md
3. test-engineer: docs/pr-reviewers/test-engineer.md

Merge them into GO/NO-GO with deterministic evidence, blockers,
recommended fixes, accepted risks, coverage gaps, verification evidence,
and rollback notes.
MSG
